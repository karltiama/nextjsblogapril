## Context

After switching project page imports to `projectsDataV2`, the existing
test mock still targeted `projectsData`, causing a Jest mock
initialization error.

## Goal

- **Goal:** Fix project page tests after v2 data source migration.
- **Inputs:** `__tests__/components/project-page.test.tsx`.
- **Output/Done:** Test mocks `projectsDataV2` with `projectsV2`.

## Files Changed

- `__tests__/components/project-page.test.tsx`

## Implementation Notes (key logic explained)

- Updated Jest module mock path:
  - from `@/content/projects/projectsData`
  - to `@/content/projects/projectsDataV2`
- Updated mocked export key:
  - from `projects`
  - to `projectsV2`

## Key Decisions (and alternatives considered)

- **Mock new source directly:** smallest targeted fix with no behavior
  change to test cases.

## Assumptions

- Test data shape remains compatible with page render assertions.

## Failure Modes

- If page import path changes again, this mock must be kept in sync.

## How to Test (commands + steps)

1. `npm run test -- project-page`
2. Confirm all tests in that suite pass.

## Next Steps

- Optionally add a shared test helper for project mocks to avoid path
  drift.

## Dependencies / env vars

- **None added.**

## Verification Checklist

- [ ] Test mock path matches page import path.
- [ ] Mocked export name matches module export (`projectsV2`).
- [ ] Project page test suite passes.
