## Context

Featured project cards were still reading the legacy projects array.
To keep data usage consistent, list and detail views should point to
the same source.

## Goal

- **Goal:** Move featured list to the new `projectsV2` array.
- **Inputs:** `components/project-list.tsx` import and
  `projectsDataV2.ts`.
- **Output/Done:** `ProjectList` uses `projectsV2` (aliased as
  `projects`).

## Files Changed

- `components/project-list.tsx`

## Implementation Notes (key logic explained)

- Replaced legacy import path with
  `../content/projects/projectsDataV2`.
- Left filtering/render logic unchanged.

## Key Decisions (and alternatives considered)

- **No logic changes:** minimized risk by only changing data source
  import.

## Assumptions

- Card component still relies on shared base fields present in both
  arrays.

## Failure Modes

- If v2 export is renamed, this import fails at build time.

## How to Test (commands + steps)

1. `npm run dev`
2. Open home/projects section and confirm featured cards render.

## Next Steps

- Update tests mocking old data path if they validate source imports.

## Dependencies / env vars

- **None added.**

## Verification Checklist

- [ ] Featured projects render in the same order as before.
- [ ] No runtime error in `ProjectList`.
- [ ] Data source path now points to `projectsDataV2`.
