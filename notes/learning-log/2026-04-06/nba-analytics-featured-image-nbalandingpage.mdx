## Context

Featured project cards read `imageSrc` from `content/projects/projectsData.ts`. The NBA Analytics App (second featured card, reversed layout with `md:order-2` on the image) still pointed at `/businessplan.png`.

## Goal

Use `nbalandingpage.png` from `public/` for that card’s hero image.

## Files Changed

- `content/projects/projectsData.ts`

## Implementation Notes (key logic explained)

- Set `imageSrc` to `/nbalandingpage.png` (Next.js serves files in `public/` from the site root).
- Adjusted `altText` to describe the new screenshot.

## Key Decisions (and alternatives considered)

- **Data-only change:** `project-card.tsx` already uses `imageSrc`; no component change required.

## Assumptions

- `public/nbalandingpage.png` exists (user added it).

## Failure Modes

- Typo in filename or wrong casing on case-sensitive hosts breaks the image; path must match the file in `public/` exactly.

## How to Test (commands + steps)

1. `npm run dev`
2. Open `/` (or `/projects`), find “NBA Analytics App” in Featured Systems.
3. Confirm the card shows the new landing screenshot.

## Next Steps

- If the dedicated project page uses the same `imageSrc`, it will update automatically; spot-check `/projects/nba-analytics-app` (or generated slug).

## Verification Checklist

- [ ] NBA Analytics featured card displays `nbalandingpage.png`.
- [ ] No broken image icon in dev.
- [ ] Alt text reads sensibly in devtools or a screen reader sample.
