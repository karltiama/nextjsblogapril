## Context
The hero CTA buttons continued to be unreliable in the current landing layout and the requested outcome is to remove them.

## Goal
Remove the hero buttons from the landing page.

## Files Changed
- `app/page.tsx`

## Implementation Notes (key logic explained)
- Removed `HomeHeroCtas` import.
- Removed `<HomeHeroCtas />` from the hero content area.
- Left all other hero text and section structure unchanged.

## Key Decisions (and alternatives considered)
- Chose direct removal per request rather than further debugging.
- Kept layout and typography intact to minimize unrelated visual changes.

## Assumptions
- Only the two hero CTA buttons should be removed.

## Failure Modes
- If a future feature expects CTA presence, it will need reintroduction.

## How to Test (commands + steps)
1. Run `npm run dev`
2. Open `/`
3. Confirm hero shows heading and paragraph only (no CTA buttons)
4. Confirm page renders blog and project sections normally

## Next Steps
- Optionally adjust hero spacing now that button row is removed.
