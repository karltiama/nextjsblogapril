## Context
After removing CTA usage from the home page, the dedicated CTA component became unused.

## Goal
Delete the unused hero CTA component to keep the codebase clean.

## Files Changed
- `components/home-hero-ctas.tsx` (deleted)

## Implementation Notes (key logic explained)
- Removed the now-unreferenced component file.
- No runtime behavior change beyond already-removed hero buttons.

## Key Decisions (and alternatives considered)
- Chose deletion instead of leaving dead code to reduce maintenance overhead.
- Alternative: keep for future reuse; rejected because current request is explicit removal.

## Assumptions
- There are no other imports of `home-hero-ctas`.

## Failure Modes
- If another branch or stale file references this component, merge conflicts/import errors may occur later.

## How to Test (commands + steps)
1. Run `npm run dev`
2. Confirm app compiles
3. Open `/` and verify no hero CTA buttons are rendered

## Next Steps
- If desired, tighten hero spacing now that CTA block is removed.
