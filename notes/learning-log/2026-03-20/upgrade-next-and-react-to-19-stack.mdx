## Context
You requested moving the project to React 19 while updating Next.js.

## Goal
Upgrade the core framework/runtime stack to Next.js 16 + React 19.

## Files Changed
- `package.json`
- `package-lock.json`

## Implementation Notes (key logic explained)
Upgraded:
- `next` to `^16.2.0`
- `react` / `react-dom` to `^19.2.4`
- `@types/react` / `@types/react-dom` to React 19-compatible versions
- `eslint-config-next` to `^16.2.0`

Also removed the old React 18 override pins in a prior step to allow resolution.

## Key Decisions (and alternatives considered)
- Decision: perform framework + runtime upgrade together.
- Alternative: React 19 only without Next upgrade. Rejected because you asked to update Next.js and React 19 together.

## Assumptions
- Project environment supports Next.js 16 runtime requirements.

## Failure Modes
- Older ecosystem packages may still carry React 18 peer ranges and require follow-up upgrades.
- ESLint ecosystem may need version alignment with Next.js plugin peers.

## How to Test (commands + steps)
1. Run `npm run build`.
2. Run `npm run dev`.
3. Open `/` and `/terrain`.

## Next Steps
- Resolve remaining peer-version compatibility warnings and confirm runtime stability.
