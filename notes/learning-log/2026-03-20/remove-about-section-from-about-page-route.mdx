## Context

`AboutSection` was temporarily added to the top of `app/about/page.tsx`; placement belongs under `TerrainHero` instead.

## Goal

Restore `/about` to its previous layout without `AboutSection`.

## Files Changed

- `app/about/page.tsx`

## Implementation Notes (key logic explained)

- Removed `AboutSection` import and the fragment wrapper; page root is again a single `div.container`.

## Key Decisions (and alternatives considered)

- **Decision:** remove from about page to avoid duplicate About content.
- **Alternative:** keep on both routes — rejected per user correction.

## Assumptions

- Long-form about content on `/about` remains the primary “about” page for SEO and detail.

## Failure Modes

- Users expecting About hero on `/about` no longer see it there — it lives under `/terrain` now.

## How to Test (commands + steps)

1. Open `/about` — no dark stagger hero at top.
2. Open `/terrain` — scroll to About below terrain.

## Next Steps

- Optional: add a nav link “Terrain” or “About preview” pointing to `/terrain#about`.

## Verification Checklist

- [ ] `/about` builds and matches prior structure
- [ ] No unused import of `AboutSection`
