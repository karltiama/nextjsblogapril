## Context

You wanted the dot-grid overlay in `AboutSection` to match your reference snippet exactly.

## Goal

Match `className` and inline `style` for the radial dot pattern layer.

## Files Changed

- `components/about-section.tsx`

## Implementation Notes (key logic explained)

- Removed `z-0` from the pattern layer so it matches `absolute inset-0 opacity-[0.16]` only.
- Kept the same `backgroundImage` / `backgroundSize` values; used single-quoted string literals in the `style` object to mirror your snippet.

## Key Decisions (and alternatives considered)

- **Decision:** drop explicit `z-0` on the grid — stacking still works with `isolate` on the section plus `z-[2]` / `z-[3]` on blend and content.

## Assumptions

- Visual output is unchanged aside from stacking order nuance at layer boundaries.

## Failure Modes

- If a future layer omits z-index, paint order could differ — reintroduce `z-0` on the grid if needed.

## How to Test (commands + steps)

1. `npm run dev` → `/terrain`, scroll to About — dot grid should match prior look.

## Next Steps

- None unless layering looks wrong on a specific browser.

## Verification Checklist

- [ ] Dot pattern visible across the section
- [ ] Text still reads above the pattern
