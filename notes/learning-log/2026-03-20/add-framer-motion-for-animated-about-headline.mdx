## Context
The About headline needs animated rotating suffix text using `AnimatePresence`, `motion`, and reduced-motion handling.

## Goal
Add required animation dependency support.

## Files Changed
- `package.json`
- `package-lock.json`

## Implementation Notes (key logic explained)
- Installed `framer-motion` to provide:
  - `AnimatePresence`
  - `motion`
  - `useReducedMotion`

## Key Decisions (and alternatives considered)
- Decision: use Framer Motion as requested for clean enter/exit transitions.
- Alternative: CSS-only animation with manual class toggling. Rejected to match requested behavior and reduced-motion API.

## Assumptions
- Bundle size impact of Framer Motion is acceptable for this animated headline use case.

## Failure Modes
- If Framer Motion import path changes in future major versions, component imports may require updates.

## How to Test (commands + steps)
1. `npm run dev`
2. Load route containing `AboutSection`.
3. Confirm rotating headline text animates without errors.

## Next Steps
- Implement rotating suffix logic in `components/about-headline.tsx`.
