## Context
Terrain hero was mounted on `/`, but the goal changed to hosting it on a dedicated page.

## Goal
Remove terrain hero from the home page while keeping the rest of home content unchanged.

## Files Changed
- `app/page.tsx`

## Implementation Notes (key logic explained)
Removed the `HomeTerrainHero` import and its render call from the top of the home page fragment. No other layout/content sections were modified.

## Key Decisions (and alternatives considered)
- Decision: remove only terrain-related lines from home page.
- Alternative: keep a mini terrain preview on home. Deferred because request is to place terrain on a separate page.

## Assumptions
- Existing home hero and sections should remain exactly as-is.

## Failure Modes
- If any code still assumes terrain is on home, UX expectations may differ.

## How to Test (commands + steps)
1. Run `npm run dev`.
2. Open `/`.
3. Confirm terrain section no longer appears.

## Next Steps
- Add dedicated terrain route page and render full-height terrain there.
